# Bordered Teacher-Mothers · ACWS 2026

Static GitHub Pages package for Zhuo Chang's ACWS panel presentation.

Entry point:

- `index.html`

Assets:

- `images/acws-logo.png`

Deployment:

- Put these files at the root of a GitHub Pages repository, or inside the repository's `/docs` folder if Pages is configured to serve from `/docs`.
